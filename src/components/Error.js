import React from 'react'
import styled from 'styled-components'

const StyledError = styled.p``

const Error = props => {
    return (
        <StyledError>Check your internet connection</StyledError>
    )
}

export default Error